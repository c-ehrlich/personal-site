import type { NextPage } from 'next';
import PageSectionContainer from '../components/PageSectionContainer';

const projects: NextPage = () => {
  return (
    <PageSectionContainer>projects page</PageSectionContainer>
  )
}

export default projects
